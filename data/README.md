# data
This folder contains
- raw data
- sample traces
- figures
- tables
- summary statistics
- diagnostic logs

Note: Do not use git lfs! Do not track files! Just add manually via
```
git add -f filename
git commit -m "manually add"
```
followed by 
```
git rm --cached filename
git commit -m "stop tracking"
```
